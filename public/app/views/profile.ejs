<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/graphics/logo/icon.svg">
    <title>@<%=username%> on Discbro</title>

    <!-- CSS for all pages -->
    <link rel="stylesheet" href="/theme.css" />
    <link rel="stylesheet" href="/global.css" />

    <!-- CSS for this page -->
    <link rel="stylesheet" href="./style.css" />
    
    <!-- Scripts for all pages -->
    <script src="/theme.js"></script>
</head>
<body>
    <div id="userbox">
        <img src="<%=profile_picture%>" />

        <div>
            <h1><%=display_name%></h1>
            <h2><%=username%> [ID: <%=id%>]</h2>
            <p><%=bio%></p>
        </div>
    </div>

    <p>The viewer is <%=viewer_id%></p>

    <% if (id == viewer_id) { %>
        <p>You are the goat!</p>
    <% } %>
    
</body>
</html>